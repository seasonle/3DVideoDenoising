[file,path] = uigetfile('*.mat','Select denoising results');
load(fullfile(path,file));

[C,matches] = strsplit(file,'\d*-\d*','DelimiterType','RegularExpression');

createVideo3D(results.data.data,path,C)